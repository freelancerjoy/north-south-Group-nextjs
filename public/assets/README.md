Place the provided hero images into this folder with the following filenames:

- hero_titanic.jpg
- hero_beach_wide.jpg
- premium_apartments.jpg
- plots.jpg
- build_future.jpg

Notes:
- This project uses Vite; files in `public/` are served at the site root. The app references these images as `/assets/<filename>`.
- After copying the images here, restart the dev server if it was running (`npm run dev`).
- If you prefer different names, update `frontend/src/pages/Home.jsx` to match.
